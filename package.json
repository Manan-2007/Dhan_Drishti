{
  "name": "dhan-drishti",
  "version": "0.0.0",
  "private": true,
  "description": "Private, self-hosted investment portfolio & wealth tracker",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint",
    "dev": "pnpm -r --parallel dev"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild", "@node-rs/argon2", "@libsql/client"]
  },
  "packageManager": "pnpm@10.33.4"
}
